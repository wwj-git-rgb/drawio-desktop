interface:
  display_name: "Drawio Desktop"
  short_description: "Repo-specific patterns and workflows for drawio-desktop"
  default_prompt: "Use the drawio-desktop repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true